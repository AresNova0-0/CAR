/* LuxeCar production connectors.
   GitHub Pages can host the frontend, but secrets MUST stay server-side.
   Fill only public Supabase URL + anon key here. Stripe secret and email keys belong in backend/.env. */
window.LUXECAR_CONFIG={
  SUPABASE_URL:'',
  SUPABASE_ANON_KEY:'',
  API_BASE:'',
  CURRENCY:'VND',
  DEMO_MODE:true
};
